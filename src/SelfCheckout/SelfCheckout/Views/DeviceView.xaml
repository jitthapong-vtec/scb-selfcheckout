<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:SelfCheckout.Controls"
             xmlns:i18n="clr-namespace:SelfCheckout.Services.Localize"
             xmlns:effects="clr-namespace:SelfCheckout.Effects"
             mc:Ignorable="d"
             Visual="Material"
             x:Name="this"
             x:Class="SelfCheckout.Views.DeviceView">
    <ContentView.Content>
        <Grid>
            <Frame Padding="0" Margin="-2,0,-2,0" VerticalOptions="Start">
                <StackLayout Margin="12" Spacing="12">
                    <StackLayout Orientation="Horizontal">
                        <Label Text="{i18n:Translate Name}" HorizontalOptions="StartAndExpand"/>
                        <Label Text="{Binding CustomerData.Person.EnglishName}" HorizontalTextAlignment="End"/>
                    </StackLayout>
                    <BoxView HeightRequest="1" HorizontalOptions="FillAndExpand" BackgroundColor="{StaticResource Gray200}"/>
                    <StackLayout Orientation="Horizontal">
                        <Label Text="{i18n:Translate DateTime}" HorizontalOptions="StartAndExpand"/>
                        <Label Text="{Binding NowDateTime}" HorizontalTextAlignment="End"/>
                    </StackLayout>
                    <BoxView HeightRequest="1" HorizontalOptions="FillAndExpand" BackgroundColor="{StaticResource Gray200}"/>
                    <StackLayout Orientation="Horizontal">
                        <Label Text="{i18n:Translate FlightNo}" HorizontalOptions="StartAndExpand"/>
                        <Label Text="{Binding CustomerData.Person.FlightCode}" HorizontalTextAlignment="End"/>
                    </StackLayout>
                    <BoxView HeightRequest="1" HorizontalOptions="FillAndExpand" BackgroundColor="{StaticResource Gray200}"/>
                    <StackLayout Orientation="Horizontal">
                        <Label Text="{i18n:Translate MobileNo}" HorizontalOptions="StartAndExpand"/>
                        <Label Text="{Binding MobileNo}" HorizontalTextAlignment="End"/>
                    </StackLayout>
                </StackLayout>
            </Frame>
        </Grid>
    </ContentView.Content>
</ContentView>